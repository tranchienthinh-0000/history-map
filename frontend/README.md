# History Map Web (Frontend)

### 1) Cấu hình
- Tạo file `.env` từ `.env.example` và điền:
 
  - `VITE_API=http://localhost:4000` (hoặc URL API khi deploy)

### 2) Chạy web
```bash
npm install
npm run dev
```
Mở `http://localhost:5173` để xem bản đồ.
